#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <algorithm>

int main(int argc, char *argv[]){
    int n, m;
    scanf("%d%d", &n, &m);
    int shop[n];
    for (int i=0; i<n; i++) {
        scanf("%d", &shop[i]);
    }
    return 0;
}
